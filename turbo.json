{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "check-types": {},
    "lint": {},
    "format": {},
    "test": {},
    "coverage": {
      "outputs": ["coverage/**"]
    },
    "e2e": {
      "outputs": ["playwright-report/**", "test-results"]
    }
  }
}
